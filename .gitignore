# Secrets — NEVER commit these
.env
.env.*

# Python
__pycache__/
*.pyc

# Jupyter checkpoints
.ipynb_checkpoints/

# Generated vector store (binary, regenerable from the PDFs)
data/vector_store/
notebooks/data/
faiss_store/

# Claude Code local config — never commit
.claude/

# Frontend lives in its own repo — don't track here
crux-document-intelligence/
package-lock.json
